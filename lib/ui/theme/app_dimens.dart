/// 앱 전체에서 사용하는 레이아웃/여백 상수
class AppDimens {
  AppDimens._();

  // 페이지 기본 패딩
  static const double pagePadding = 16.0;

  // 카드 간격 (고객/프론티어 리스트)
  static const double cardSpacing = 12.0;
  static const double cardSpacingLarge = 16.0;
  static const double customerCardRadius = 16.0;
  static const double searchBarHeight = 50.0;
  /// 필터/상태 pill (지역 필터, 영업상태 등) radius
  static const double filterPillRadius = 20.0;

  // 카드 radius
  static const double cardRadius = 20.0;
  static const double cardRadiusLarge = 24.0;

  // 입력창/검색바 radius
  static const double inputRadius = 16.0;
  static const double inputRadiusLarge = 20.0;

  // 그림자 (부드러운 elevation - SOS 2.0)
  static const double shadowBlur = 12.0;
  static const double shadowBlurLarge = 15.0;
  static const double shadowOpacity = 0.10;
  static const double shadowOpacityLarge = 0.12;

  // 터치 영역 최소 크기
  static const double minTouchTarget = 44.0;

  // 배너 높이
  static const double bannerHeight = 240.0;

  // BottomNav pill radius
  static const double pillRadius = 999.0;
}
